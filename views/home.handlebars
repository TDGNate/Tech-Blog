<div class="home">
	<div class="container">
		<h1>Home</h1>
		<p>Live Posts:</p>
		<hr>
		<div class="container-sub">

			{{#each posts as |post|}}
				{{!-- {{> postCard}} --}}
				{{> postCard}}
			{{/each}}

		</div>
	</div>
</div>